{% extends 'admin/template.twig' %}

{% block content %}
  <div class="row">
    <div class="col-lg-12">
      <h1 class="page-header">Supplier <a href="{{BASEURL}}/admin/supplier/tambah" class="btn btn-primary pull-right"><i class="fa fa-plus fa-fw"></i> Tambah Data</a></h1>
    </div>
    <!-- /.col-lg-12 -->
  </div>
  <!-- /.row -->
  <div class="row">
    <div class="col-lg-12">
      <div class="panel panel-default">
        <div class="panel-heading">
          List Supplier
        </div>
        <!-- /.panel-heading -->
        <div class="panel-body">
          <table width="100%" class="table table-striped table-bordered table-hover" id="myTable">
            <thead>
              <tr>
                <th>Nama Supplier</th>
                <th>Jenis Usaha</th>
                <th>Kota</th>
                <th>Alamat</th>
                <th>Telepon</th>
                <th>Aksi</th>
              </tr>
            </thead>
            <tbody>
              {% for rs in rec %}
                <tr>
                  <td>{{rs.nama_supplier}}</td>
                  <td>{{rs.jenis_usaha}}</td>
                  <td>{{rs.kota.tipe}} {{rs.kota.nama_kota}}</td>
                  <td>{{rs.alamat}}</td>
                  <td>{{rs.telepon}}</td>
                  <td>{{rs.supplier_id}}</td>
                </tr>
              {% endfor %}
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </div>
{% endblock %}